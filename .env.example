# ═══════════════════════════════════════════════════════════════════════════════
# Apollos AI — Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Full reference: https://docs.apollosai.dev/advanced/envvars
#
# Copy this file to .env and fill in the values.
#   cp .env.example .env
#
# Required variables are marked with [REQUIRED].
# Generation commands are provided for secrets and keys.
# ═══════════════════════════════════════════════════════════════════════════════

# ─── Domain Configuration ─────────────────────────────────────────────────────
# Base domain for CORS, CSRF, and email templates.
APOLLOS_DOMAIN=apollosai.dev
APOLLOS_ALLOWED_DOMAIN=apollosai.dev
# Set to true if running without HTTPS (e.g., local dev behind no reverse proxy)
APOLLOS_NO_HTTPS=false

# ─── Django [REQUIRED for production] ─────────────────────────────────────────
# Generate: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Or:       openssl rand -base64 50
APOLLOS_DJANGO_SECRET_KEY=change-me-in-production

# ─── Admin Account [REQUIRED for non-interactive / Docker] ────────────────────
# These are used on first run to create the admin superuser.
# Generate password: openssl rand -base64 32
APOLLOS_ADMIN_EMAIL=admin@apollosai.dev
APOLLOS_ADMIN_PASSWORD=change-me-in-production

# ─── Email (Resend) ──────────────────────────────────────────────────────────
# Required for magic link auth and notifications. Get key at https://resend.com
RESEND_API_KEY=
RESEND_EMAIL=noreply@apollosai.dev
RESEND_AUDIENCE_ID=
APOLLOS_SUPPORT_EMAIL=support@apollosai.dev

# ─── Database (PostgreSQL) ────────────────────────────────────────────────────
POSTGRES_DB=apollos
POSTGRES_USER=postgres
# Generate: openssl rand -base64 32
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Set to true to use embedded pgserver (no external PostgreSQL needed)
# USE_EMBEDDED_DB=true

# ─── LLM Providers (at least one required for chat) ──────────────────────────
# OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Uncomment to use Ollama or other OpenAI-compatible API
# OPENAI_BASE_URL=http://host.docker.internal:11434/v1/

# Anthropic: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=

# Google Gemini: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# ─── Embedding Model ─────────────────────────────────────────────────────────
# Default uses local thenlper/gte-small (no API key needed).
# To use OpenAI embeddings: set MODEL, API_TYPE=openai, and API_KEY.
# APOLLOS_EMBEDDING_MODEL=text-embedding-3-small
# APOLLOS_EMBEDDING_DIMENSIONS=1536
# APOLLOS_EMBEDDING_API_TYPE=openai
# APOLLOS_EMBEDDING_API_KEY=
# APOLLOS_EMBEDDING_ENDPOINT=
# APOLLOS_CROSS_ENCODER_MODEL=mixedbread-ai/mxbai-rerank-xsmall-v1

# ─── Chat Model Lists ────────────────────────────────────────────────────────
# Override default models created on first bootstrap. Comma-separated.
# Set to empty value to skip creating models for that provider.
# APOLLOS_OPENAI_CHAT_MODELS=gpt-4o-mini,gpt-4.1,o3,o4-mini
# APOLLOS_GEMINI_CHAT_MODELS=gemini-2.0-flash,gemini-2.5-flash,gemini-2.5-pro,gemini-2.5-flash-lite
# APOLLOS_ANTHROPIC_CHAT_MODELS=claude-sonnet-4-0,claude-3-5-haiku-latest

# ─── Server Chat Slots ───────────────────────────────────────────────────────
# Override which model is assigned to each server-wide chat slot.
# Values must match an existing ChatModel name.
# WARNING: These override admin panel changes on every restart.
# Slots marked [FREE] require FREE price tier models.
# APOLLOS_DEFAULT_CHAT_MODEL=gpt-4o-mini          # [FREE] Also sets chat_advanced unless below is set
# APOLLOS_ADVANCED_CHAT_MODEL=gpt-4.1
# APOLLOS_THINK_FREE_FAST_MODEL=gpt-4o-mini       # [FREE]
# APOLLOS_THINK_FREE_DEEP_MODEL=o3                # [FREE]
# APOLLOS_THINK_PAID_FAST_MODEL=gpt-4.1
# APOLLOS_THINK_PAID_DEEP_MODEL=o3

# ─── Bootstrap Configuration ─────────────────────────────────────────────────
# Path to a JSONC file for complete model setup. See bootstrap.example.jsonc.
# Supports ${ENV_VAR} interpolation, // comments, and trailing commas.
# APOLLOS_BOOTSTRAP_CONFIG=/path/to/bootstrap.jsonc

# ─── Google OAuth ─────────────────────────────────────────────────────────────
# Required for Google Sign-In. Get credentials at https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# ─── Online Search (at least one for /search tool) ───────────────────────────
# SearXNG is included in Docker Compose (auto-configured).
# APOLLOS_SEARXNG_URL=http://search:8080

# Google Custom Search: https://developers.google.com/custom-search/v1/overview
# GOOGLE_SEARCH_API_KEY=
# GOOGLE_SEARCH_ENGINE_ID=

# Serper.dev: https://serper.dev
# SERPER_DEV_API_KEY=

# ─── Web Scraping ────────────────────────────────────────────────────────────
# Firecrawl: https://firecrawl.dev
# FIRECRAWL_API_KEY=
# FIRECRAWL_API_URL=https://api.firecrawl.dev

# Exa (neural search): https://exa.ai
# EXA_API_KEY=
# EXA_API_URL=https://api.exa.ai

# Olostep: https://olostep.com
# OLOSTEP_API_KEY=
# OLOSTEP_API_URL=https://agent.olostep.com/olostep-p2p-incomingAPI

# ─── Code Execution ──────────────────────────────────────────────────────────
# Terrarium sandbox (included in Docker Compose, auto-configured)
# APOLLOS_TERRARIUM_URL=http://sandbox:8080

# E2B cloud sandbox (alternative to Terrarium): https://e2b.dev
# E2B_API_KEY=
# E2B_TEMPLATE=code-interpreter-stateful

# ─── Computer Use / Operator ─────────────────────────────────────────────────
# APOLLOS_OPERATOR_ENABLED=false
# APOLLOS_OPERATOR_ITERATIONS=100
# APOLLOS_CDP_URL=

# ─── Notion Integration ──────────────────────────────────────────────────────
# https://developers.notion.com/docs/create-a-notion-integration
# NOTION_OAUTH_CLIENT_ID=
# NOTION_OAUTH_CLIENT_SECRET=
# NOTION_REDIRECT_URI=

# ─── Voice / Text-to-Speech ──────────────────────────────────────────────────
# ElevenLabs: https://elevenlabs.io
# ELEVEN_LABS_API_KEY=

# ─── Payments (Stripe) — cloud/SaaS only ─────────────────────────────────────
# STRIPE_API_KEY=
# STRIPE_SIGNING_SECRET=
# STRIPE_APOLLOS_PRODUCT_ID=
# APOLLOS_CLOUD_SUBSCRIPTION_URL=

# ─── SMS / Phone Verification (Twilio) ───────────────────────────────────────
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_VERIFICATION_SID=

# ─── AWS / S3 Storage ────────────────────────────────────────────────────────
# Generate credentials: https://docs.aws.amazon.com/IAM/latest/UserGuide/id_credentials_access-keys.html
# AWS_ACCESS_KEY=
# AWS_SECRET_KEY=
# AWS_IMAGE_UPLOAD_BUCKET=
# AWS_USER_UPLOADED_IMAGES_BUCKET_NAME=

# ─── Advanced / Debugging ────────────────────────────────────────────────────
# APOLLOS_DEBUG=false
# APOLLOS_TELEMETRY_DISABLE=false
# APOLLOS_LLM_SEED=42                    # Fixed seed for deterministic LLM output (testing)
# APOLLOS_RESEARCH_ITERATIONS=5          # Max iterations for research mode
# PROMPTRACE_DIR=/path/to/traces         # Enable prompt tracing for debugging
