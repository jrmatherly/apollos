# Apollos Configuration
# See documentation for all options: https://docs.apollosai.dev/get-started/setup

# Domain Configuration (required for production)
APOLLOS_DOMAIN=apollosai.dev
APOLLOS_ALLOWED_DOMAIN=apollosai.dev
APOLLOS_NO_HTTPS=false

# Django
APOLLOS_DJANGO_SECRET_KEY=change-me-in-production

# Email (Resend)
RESEND_API_KEY=
RESEND_EMAIL=noreply@apollosai.dev
RESEND_AUDIENCE_ID=
APOLLOS_SUPPORT_EMAIL=support@apollosai.dev

# Database
POSTGRES_DB=apollos
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# LLM Providers (at least one required)
OPENAI_API_KEY=
# OPENAI_BASE_URL=http://host.docker.internal:11434/v1/ # Uncomment to use Ollama or OpenAI compatible API
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Embedding Model Configuration
# Controls the embedding model used for semantic search. Default is local thenlper/gte-small.
# To use OpenAI embeddings: set MODEL, API_TYPE=openai, and API_KEY.
# APOLLOS_EMBEDDING_MODEL=text-embedding-3-small
# APOLLOS_EMBEDDING_DIMENSIONS=1536
# APOLLOS_EMBEDDING_API_TYPE=openai
# APOLLOS_EMBEDDING_API_KEY=
# APOLLOS_EMBEDDING_ENDPOINT=
# APOLLOS_CROSS_ENCODER_MODEL=mixedbread-ai/mxbai-rerank-xsmall-v1

# Chat Model List Configuration
# Override which chat models are created on first bootstrap. Comma-separated.
# Set to empty value to skip creating models for that provider (e.g. APOLLOS_OPENAI_CHAT_MODELS=).
# APOLLOS_OPENAI_CHAT_MODELS=gpt-4o-mini,gpt-4.1,o3,o4-mini
# APOLLOS_GEMINI_CHAT_MODELS=gemini-2.0-flash,gemini-2.5-flash,gemini-2.5-pro,gemini-2.5-flash-lite
# APOLLOS_ANTHROPIC_CHAT_MODELS=claude-sonnet-4-0,claude-3-5-haiku-latest

# Server Chat Slot Configuration
# Override which chat model is assigned to each ServerChatSettings slot.
# Values must match an existing ChatModel name. Slots chat_default, think_free_fast,
# and think_free_deep require FREE price tier models (STANDARD tier will be skipped with a warning).
# APOLLOS_DEFAULT_CHAT_MODEL also sets chat_advanced unless APOLLOS_ADVANCED_CHAT_MODEL is set.
# NOTE: These env vars are re-applied on every server restart, overriding any
# manual changes made via the admin panel. Remove the env var to stop overriding.
# APOLLOS_DEFAULT_CHAT_MODEL=gpt-4o-mini
# APOLLOS_ADVANCED_CHAT_MODEL=gpt-4.1
# APOLLOS_THINK_FREE_FAST_MODEL=gpt-4o-mini
# APOLLOS_THINK_FREE_DEEP_MODEL=o3
# APOLLOS_THINK_PAID_FAST_MODEL=gpt-4.1
# APOLLOS_THINK_PAID_DEEP_MODEL=o3

# Bootstrap Configuration File
# Path to a JSONC file that defines the complete model configuration (providers, models, slots, embedding).
# When set, the bootstrap config is applied first, then individual env vars above act as overrides.
# See docs for the config schema. Supports JSONC (comments, trailing commas) and ${ENV_VAR} interpolation.
# APOLLOS_BOOTSTRAP_CONFIG=/path/to/bootstrap.jsonc
